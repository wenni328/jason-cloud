<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloud.eureka.mapper.UserMapper">

    <select id="selectPageList" resultType="com.cloud.eureka.entity.UserModel">
    SELECT u.name,u.id id,u.password,u.salt,b.* FROM `user` u
LEFT JOIN blog b ON u.id=b.blog_id
AND u.`name` LIKE "%"#{name}"%"
</select>

</mapper>